# Overlays

Vendor repositories are read-only. Machine-independent routing aliases and security hooks belong here. Generated active copies record source commit and overlay hash in `manifests/skills.json`.
